# Default values for sprout-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

globals:
  kargo-demo-app-charts:
    enabled: true

kargo-demo-app-charts:
  image:
    tag: "0.1.19"
  replicaCount: 1
kargo-show-app-charts:
  image:
    tag: "0.0.3"
  replicaCount: 1